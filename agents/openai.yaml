interface:
  display_name: "Obsidian CLI Memory Bank"
  short_description: "Retrieve and maintain concise Obsidian project memory"
  default_prompt: "Use obmem to retrieve only relevant prior context and record a concise durable outcome when warranted."
